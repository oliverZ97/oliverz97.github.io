interface Paths {
  [key: string]: string;
}

export default {
  HOME: "/",
  TCG: "/tcg",
  COLLECTION: "/collection",
  SANDBOX: "/sandbox",
  NOT_FOUND: "*",
  KEEPORPASS: "keep-or-pass"
} as Paths;
